+ FastAPI (Local endpoint, to receive the Open WebUI Prompt.)
+ Setup OpenHubUI with Docker containers instead of python
+ Nomic embed text does not need to be local. It can be an only API, since only styleguides are stored inside. For testing nomic embed text can also run locally
+ .gitignore 